@model MembershipUserCollection

@{
    ViewBag.Title = "Manage Users";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Manage Users</h2>

<table class="table table-bordered">
	<thead>
		<tr>
			<th>Username</th>
			<th>Email</th>
			<th>Last login date</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach(MembershipUser user in Model) {
			<tr>
				<td>@user.UserName</td>
				<td>@user.Email</td>
				<td>@user.LastLoginDate</td>
				<td>
                    <a href="@Url.Action("EditUser", new { username = user.UserName })" class="btn btn-info" style="margin-left: 10px"><i class="icon-edit icon-white"></i>&nbsp;Edit User</a>
				</td>
			</tr>
		}
	</tbody>
</table>
<div style="margin: 10px">
    <a href="@Url.Action("Register")" class="btn btn-primary"><i class="icon-user icon-white"></i>&nbsp;Add user</a>
</div>
